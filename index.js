/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SplassScreen from './src/screens/splash_screen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SplassScreen);
