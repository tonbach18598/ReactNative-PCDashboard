import {READ_CLASS_POSTS} from '../actions/type'
let classPosts=[]

const departmentReducer=(state=classPosts,action)=>{
    switch(action.type){
        case READ_CLASS_POSTS:
            
    }
}

export default departmentReducer